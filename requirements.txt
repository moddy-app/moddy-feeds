# Runtime dependencies — pinned to compatible minor ranges for reproducible builds.
redis[hiredis]==5.0.8        # async Redis client (streams, consumer groups, SET NX)
asyncpg==0.29.0              # async PostgreSQL driver
httpx==0.27.2               # async HTTP client (YouTube/Twitch/RSS/Bluesky REST)
websockets==12.0           # Bluesky Jetstream websocket
feedparser==6.0.11         # robust RSS/Atom parsing
pydantic==2.9.2            # settings + validation
pydantic-settings==2.5.2   # 12-factor config from env
